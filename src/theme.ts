import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#3f51b5', // You can customize colors here
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default theme;
